apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: aecontrol-postgres-promoter
rules:
  - apiGroups: [postgresql.cnpg.io]
    resources: [clusters]
    resourceNames: [aecontrol-postgres-secondary]
    verbs: [get, patch]
