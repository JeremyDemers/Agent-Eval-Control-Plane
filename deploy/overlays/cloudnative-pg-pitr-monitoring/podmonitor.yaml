apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: aecontrol-postgres
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: aecontrol-postgres
  podMetricsEndpoints:
    - port: metrics
      interval: 30s
      scrapeTimeout: 10s
